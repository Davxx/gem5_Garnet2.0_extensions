#!/usr/bin/zsh

mn=$(echo 1.05)
while [ 1 -eq 1 ]; do
    load=$(awk '{print $1}'< /proc/loadavg)
    if (($load < $mn)); then
        ./batchfb2
        ./batchmesh2
        break
    fi
    sleep 10
done

