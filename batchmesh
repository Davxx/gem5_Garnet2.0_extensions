#!/usr/bin/env bash

for ((k=87;k<=101;k+=2))
do
    i=$(bc <<<"scale=2; $k / 100" )
    i="0"$i
    
    if [ $k -eq 101 ]; then
        i=1.0
    fi
    ./rungarnet 64 8 1 Mesh_XY $i uniform_random 20000
done

